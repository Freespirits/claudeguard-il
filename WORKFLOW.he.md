<div dir="rtl">

# 🛡️ ClaudeGuardIL — מדריך זרימת עבודה מלא

מדריך צעד-אחר-צעד, מההתקנה ועד הדוח והתיקון. כתוב למי שבנה אפליקציה עם כלי AI ומעולם לא למד אבטחה.
כל פקודה כאן היא בטוחה כברירת מחדל — היא **קוראת** את הקוד ולא משנה כלום, אלא אם תבקשו במפורש.

> **הכלל שמנחה את הכל:** התראה שגויה גרועה יותר מהחמצה. אם הכלי אומר שיש בעיה מאומתת — יש בעיה.
> דוח "נקי" **אינו** הוכחה לבטיחות — הוא רשומה של מה שנבדק.

---

## תוכן העניינים

1. [התקנה](#התקנה)
2. [התמונה הגדולה — הצינור](#התמונה-הגדולה--הצינור)
3. [שלב 1 · סריקה סטטית (`/cg-scan`)](#שלב-1--סריקה-סטטית-cg-scan)
4. [שלב 2 · לוגיקה עסקית (`/cg-intent`)](#שלב-2--לוגיקה-עסקית-cg-intent)
5. [שלב 3 · ייצור הגנות (`/cg-harden`)](#שלב-3--ייצור-הגנות-cg-harden)
6. [שלב 4 · החלת תיקונים (`/cg-fix`)](#שלב-4--החלת-תיקונים-cg-fix)
7. [שלב 5 · בדיקות live ו-DAST (`/cg-live`, `/cg-dast`)](#שלב-5--בדיקות-live-ו-dast-cg-live-cg-dast)
8. [איך לקרוא את הדוח](#איך-לקרוא-את-הדוח)
9. [שאלות נפוצות](#שאלות-נפוצות)

---

## התקנה

יש שתי דרכים. בחרו אחת.

### א) תוסף ל-Claude Code (המנוע המלא — מומלץ)

סורק את כל ה-repo, מריץ סורקים חיצוניים, ויכול לייצר ולהחיל תיקונים.

<div dir="ltr">

```
/plugin marketplace add Freespirits/claudeguard-il
/plugin install claudeguard-il@claudeguard-il
```

</div>

### ב) Skill ל-claude.ai / Claude Desktop (ידע + דוח)

למי שלא עובד בטרמינל. בונים קובץ zip פעם אחת ומעלים אותו ב-claude.ai דרך **הגדרות → יכולות → Skills**.
בגרסה הזו אין סריקת repo אוטומטית — מדביקים קוד וקונפיג ושואלים *"בדוק את האבטחה של האפליקציה"*.

---

## התמונה הגדולה — הצינור

לכל סריקה יש חמישה שלבים קבועים. חשוב להבין למה הם נפרדים:

<div dir="ltr">

```
מנוע → עובדות → מדרג → ממצאים → סוקרים מוסיפים שיפוט → דוח
Engine → Facts → Grader → Findings → Reviewers → Report
```

</div>

| שלב | מי | מה מותר לו לעשות |
|------|-----|------------------|
| **מנוע** | `project_model.mjs` | מפיק **עובדות** בלבד: routes, טבלאות, משתני סביבה, אתרי LLM, לקוחות Supabase. אין לו דעה על מסוכנות. |
| **מדרג** | `grader.mjs` | **הסמכות היחידה על חומרה וּודאות.** הופך עובדות לממצאים ובונה את ספר הכיסוי. |
| **סוקרים** | תת-סוכני AI | מוסיפים ממצאי **שיפוט** — לוגיקה עסקית, הרשאה שקיימת אך לא אוכפת. חסומים לרמת ודאות "סביר" ולעולם לא "מאומת". |
| **מאמת** | `finding-verifier` | **מפריך בלבד** — מסיר ממצא ע"י ציון עובדה שסותרת אותו. לעולם לא מעלה ודאות ולא מוריד חומרה. |
| **דוח** | Claude | **מרנדר** את מה שהמדרג הפיק. אינך מדרג בעצמך. |

**למה זה חשוב:** החומרה נקבעת במקום אחד בלבד. אותו repo מקבל תמיד את אותו דירוג — שתי הרצות, שני מחשבים,
שני אנשים, אותם ממצאים. זה מה שמאפשר לסמוך על התוצאה במקום להתווכח עליה.

---

## שלב 1 · סריקה סטטית (`/cg-scan`)

נקודת הכניסה. בטוחה, קריאה-בלבד, בלי רשת ובלי סיסמאות.

<div dir="ltr">

```
/cg-scan
```

</div>

מריצים אותה בשורש הפרויקט. הכלי:

1. **בונה מודל** של הפרויקט — גרף ה-imports, גבול לקוח/שרת, זרימת משתני סביבה, כל route וטבלה.
2. **מדרג** כל נושא: `pass` / `fail` / `undeterminable` (לא ניתן להכריע) / `allowlisted` (אושר במפורש).
3. **מריץ סורקים חיצוניים** אם מותקנים — `gitleaks`, `semgrep`, `npm audit`, ו-`snyk` (אם מחוברים).
   הכלי לעולם לא מתקין כלום בכפייה.
4. **מפעיל סוקרי AI** על מה שהחוקים לא יכלו להכריע.
5. **מרנדר דוח** דו-לשוני.

### מה לגבי Snyk?

אם Snyk מותקן ומחובר (`SNYK_TOKEN`), הכלי מריץ אותו לצד `npm audit` ומשלב את התוצאות.
**שימו לב:** `snyk code` (ניתוח SAST עמוק) **מעלה את קוד המקור שלכם לשרתי Snyk**. לכן הוא כבוי כברירת מחדל,
והכלי ישאל אתכם שאלה אחת ברורה לפני שהוא מפעיל אותו. כל שאר בדיקות Snyk רצות מקומית.

---

## שלב 2 · לוגיקה עסקית (`/cg-intent`)

זה השלב שרוב הכלים לא יכולים לעשות, והוא שווה את שתי הדקות.

**הבעיה:** סורק סטטי לא יכול לדעת מה האפליקציה **אמורה** להרשות. "משתמש א' יכול לקרוא את ההזמנה של
משתמש ב'" הוא באג קריטי בחנות — ופיצ'ר מכוון בלוח ניהול. אותו קוד בדיוק, פסיקה הפוכה. רק אתם יודעים.

לכן `/cg-scan` לבדו מדווח על הלוגיקה העסקית כ**"מונח"** (assumed) — הוא ניחש את מודל הבעלות משמות
העמודות. `/cg-intent` הופך את הניחוש לאישור:

<div dir="ltr">

```
/cg-intent
```

</div>

הכלי **לא** יבקש מכם לכתוב YAML. הוא שואל שאלות בשפה פשוטה, עם אפשרויות מוכנות משמות השדות שלכם:

> **`orders`** — מי מורשה לראות שורה בטבלה הזו?
> - רק מי שיצר אותה
> - כל מי שבחברה/צוות שלו
> - כל מי שמחובר
> - כל אחד, גם מבקרים לא מחוברים
> - **אני לא יודע — דלג על הטבלה** ← תשובה לגיטימית לחלוטין

"אני לא יודע" היא תשובה מלאה — עדיף להשאיר טבלה לא-מוכרעת מאשר לנחש עליה לא נכון. יש תקרה של 6 טבלאות
כדי שהראיון לא יהיה אינסופי.

בסוף, הכלי כותב קובץ אחד — `claudeguard.intent.yml` בשורש — מראה לכם משפט פשוט לאישור *("רק מי שהזמין
יכול לראות את ההזמנה; רק מערכת התשלומים יכולה לסמן כ'שולם'")*, ומריץ `/cg-scan` שוב. עכשיו הסטטוס עובר
מ-`assumed` ל-`confirmed`, וקטגוריות שלא ניתן היה לבדוק קודם — נבדקות.

**איך זה מוזן טכנית:** אחרי שהקובץ קיים, `grader.mjs` מוצא אותו אוטומטית בשורש ה-repo. אפשר גם
במפורש: `grader.mjs <path> --intent <file>`, ולבדוק תקינות עם `--check-intent <file>`.
כל ממצא לוגיקה-עסקית חסום לרמת "סביר" ולעולם לא "מאומת" — כי בדקנו מול כוונה **מוצהרת**, לא הוכחנו אותה.

---

## שלב 3 · ייצור הגנות (`/cg-harden`)

<div dir="ltr">

```
/cg-harden
```

</div>

לוקח את הממצאים ומייצר **קוד הגנה מוכן להדבקה**, מותאם לקוד האמיתי שלכם — מדיניות RLS, middleware לאימות,
הגבלת קצב, security headers. **הפקודה הזו לא כותבת כלום** — היא רק מציגה לכם מה להדביק.

---

## שלב 4 · החלת תיקונים (`/cg-fix`)

<div dir="ltr">

```
/cg-fix              # קודם diff יבש — מראה מה ישתנה
/cg-fix --apply      # מחיל בפועל
```

</div>

מחיל את ההגנות. **תמיד מראה diff יבש קודם**, ואתם מאשרים. כשירות של זהירות, הפקודה מחילה רק תיקונים
שהם גם `confirmed` (מאומת) **וגם** מסומנים כניתנים-לתיקון-אוטומטי. כל השאר נשאר בידיים שלכם.

---

## שלב 5 · בדיקות live ו-DAST (`/cg-live`, `/cg-dast`)

**הרמות האלו שולחות תעבורת רשת אמיתית ליעד. מותרות רק על מערכת שבבעלותכם או שיש לכם הרשאה בכתב לבדוק.**

לפני שהן רצות צריך למלא קובץ הצהרה: מעתיקים `core/authorization/SCOPE.example.yml` ל-
`claudeguard.scope.yml` וממלאים. הקובץ הזה נשאר מקומי (הוא ב-gitignore) — הוא מקור הסמכות היחיד.

### Tier 1 · live פסיבית (`/cg-live`)

<div dir="ltr">

```
/cg-live https://your-app.com
```

</div>

בדיקות **קריאה-בלבד** (GET/HEAD): TLS, security headers, cookies, קבצים חשופים, קריאות ציבוריות.
דורש `passive_live.enabled: true` ו-`i_own_or_control_these_targets: true` בקובץ ה-scope.

### Tier 2 · בדיקות אקטיביות (`/cg-dast`)

<div dir="ltr">

```
/cg-dast https://your-app.com                    # dry-run — מתכנן, לא שולח
/cg-dast https://your-app.com --i-am-authorized  # שולח בפועל
```

</div>

**היו כנים עם עצמכם לגבי מה זה:** ארבע בקשות GET — בדיקת החזרת markup, גרש בודד ב-`id`, בדיקת
open-redirect, ובדיקת CSP. **זו בדיקת עשן, לא סורק.** אין crawling, אין זרימות מאומתות, אין IDOR,
אין fuzzing. לבדיקת חדירה אמיתית השתמשו ב-Burp, ZAP או Nuclei. הרמה הזו dry-run כברירת מחדל, מוגבלת
ל-2 בקשות לשנייה, ודורשת הצהרת הרשאה-בכתב.

---

## איך לקרוא את הדוח

- **התג בראש** = הפסיקה, וסופר **רק ממצאים מאומתים** (`confirmed`). ממצא P0 לא-מוכח מוצג — אבל לא
  הופך את התג לאדום. זה מכוון: אי-ודאות משולמת בשדה הוודאות, לא בהורדת החומרה.
- **"בפשטות"** — כל ממצא נפתח בשורה חסרת-ז'רגון: מה חשוף, למה זה חשוב, ומה הדבר האחד לעשות.
- **חומרה (P0–P4)** = כמה זה חמור **אם זה אמיתי**. לא מוקטן בגלל חוסר ודאות.
- **ודאות** = כמה בטוחים. `confirmed` (הוכח) / `likely` (סביר) / `needs-review` (דורש בדיקה).
- **כיסוי** — שני צירים נפרדים. *כיסוי גילוי*: מה המנוע הצליח **לראות** (קבצים שנקראו מול שדולגו).
  *כיסוי ניתוח*: איך דירג את מה שראה. שורות `undeterminable` הן רשימת העבודה — לא כישלון, אלא חוק
  שמסרב להדפיס וי שהוא לא יכול לגבות.
- **`verifyQuery`** — אם מופיע, הריצו אותו ב-SQL editor של Supabase והדביקו את התוצאה. הוא מכריע כל
  שורת RLS בעשר שניות.

---

## שאלות נפוצות

**האם זה בטוח להריץ על production?** `/cg-scan`, `/cg-harden`, `/cg-intent` — כן, לגמרי. הן קוראות
קוד ולא נוגעות ברשת. `/cg-live` ו-`/cg-dast` שולחות תעבורה ליעד ודורשות הצהרת בעלות מפורשת.

**מצא לי אפס בעיות. אני בטוח?** לא. דוח נקי הוא הוכחה שכלום לא **הוכח** — לא שכלום לא שגוי. קראו את
שורת הכיסוי: אם הרבה נושאים `undeterminable`, המשמעות היא "לא נבדק", לא "בטוח".

**מה אם אני לא יודע לענות על שאלות ה-`/cg-intent`?** בחרו "אני לא יודע — דלג". עדיף שהכלי יאמר בכנות
"לא בדקתי את זה" מאשר ינחש לא נכון.

**זה מחליף בדיקת חדירה או סקירת אבטחה מקצועית?** לא. זו **שער ראשון** — חזק יותר מסקירת PR רגילה,
חלש יותר ממומחה AppSec מנוסה. השילוב הנכון: הכלי הזה קודם, אחר כך סקירה ממוקדת-אבטחה, ולמערכות
קריטיות גם בדיקה מאומתת.

---

<div dir="ltr">

**Community project — NOT an official Anthropic product.** MIT licensed.
See [`README.he.md`](README.he.md) · [`ROADMAP.md`](ROADMAP.md)

</div>

</div>
